// 부모 컴포넌트
"use client";

import React, { useState, useEffect } from "react";
// const axios = require("axios");

export default function Ex() {
  return (
    <div>
      {/*
      아이콘 탐색 코드.
      {Array.from({ length: 14 }, (_, i) =>
        Array.from({ length: 256 }, (_, j) => (
          <img
            key={`image_${i}_${j}`}
            src={`https://cdn-lostark.game.onstove.com/efui_iconatlas/use/use_${i}_${j}.png`}
            alt={`아이템 이미지 ${i}_${j}`}
          />
        ))
      )} */}
    </div>
  );
}
